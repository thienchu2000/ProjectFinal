<div class="container mt-5">
  <h1>Orders</h1>
  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">Bot</th>
        <th scope="col">NFTs</th>
      </tr>
    </thead>
    <tbody>
      {{#each Cr}}
        <tr>
          <td>
            {{#if Bot}}
              NameBot:
              {{Bot.NameProduct}}<br />
              Price:
              {{Bot.Price}}<br />
              Created At:
              {{formatDate createdAt}}
            {{else}}
              No Bot Order
            {{/if}}
          </td>
          <td>
            {{#if Nft}}
              NFT:
              {{Nft.NameProduct}}<br />
              Price:
              {{Nft.Price}}<br />
              TokenId:
              {{TokenId}}<br />
              SmartContract:
              {{SmartContract}}<br />
              Created At:
              {{formatDate createdAt}}
            {{else}}
              No NFT Order
            {{/if}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>